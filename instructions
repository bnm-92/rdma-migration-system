how to switch gcc:

sudo su
ulimit -l unlimited
ulimit -c unlimited
sudo username

sudo update-alternatives --install /usr/bin/gcc gcc /home/share/gcc/gcc5/bin/gcc 50 --slave /usr/bin/g++ g++ /home/share/gcc/gcc5/bin/g++
export LD_LIBRARY_PATH=/home/share/gcc/gcc5/lib64


for gcc6:

sudo add-apt-repository ppa:ubuntu-toolchain-r/test
sudo apt update
sudo apt install gcc-6
sudo apt install g++-6
sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-6 60 --slave /usr/bin/g++ g++ /usr/bin/g++-6

instructions for memcached:

`sudo apt-get install libmemcached-dev` (or build from source, I think Charles did it that way)

